You've to write a set of plain text, ","(comma) seperated so that we can do search queries for the planner agent to gather web context for a given topic. These queries should be designed to retrieve relevant information from search engines or other web sources that can help the planner agent create a comprehensive research plan. The queries should be specific enough to yield useful results but broad enough to cover various aspects of the topic. 